{
  "dependencies": {
    "bootstrap": "^5.3.8",
    "browser-sync": "^3.0.4",
    "sas": "^3.0.4",
    "sass": "^1.97.3"
  },
  "scripts": {
    "start": "browser-sync start --server --files \"*.html, *.css, *.js, **/*.json, **/*.css\""
  }
}
